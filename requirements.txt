# Pinned to the versions the production image is validated against. The auth /
# transport chain (mcp, pyjwt, cryptography, uvicorn) is pinned exactly so a
# rebuild can never silently pull a different JWT/transport library. Bump
# deliberately, re-test, then update these.
mcp[cli]==1.27.2
monarchmoneycommunity==1.4.0
pyjwt[crypto]==2.13.0
cryptography==48.0.0
pyotp==2.9.0
uvicorn==0.49.0
gql>=3.4
keyring>=24.0.0
python-dotenv>=1.0.0
pydantic>=2.0.0
